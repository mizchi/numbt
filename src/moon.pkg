import {
  "mizchi/blas" as @blas,
  "moonbitlang/core/math" as @math,
}

options(
  // Link flags must be provided by the consuming package:
  // macOS: "-framework Accelerate" (fully supported)
  // Linux: "-lopenblas -lm" (experimental - LAPACK tests may have issues)
  "native-stub": [ "numbt_stub.c" ],
  "supported-targets": [ "native" ],
)
