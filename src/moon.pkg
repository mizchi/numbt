import {
  "mizchi/blas" as @blas,
  "moonbitlang/core/math" as @math,
}

options(
  // Link flags must be provided by the consuming package:
  // macOS: "-framework Accelerate"
  // Linux: "-lopenblas -llapack -llapacke -lm" (requires libopenblas-dev liblapack-dev liblapacke-dev)
  "native-stub": [ "numbt_stub.c" ],
  "supported-targets": [ "native" ],
)
